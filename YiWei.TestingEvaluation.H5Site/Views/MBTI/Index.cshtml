@{
    ViewBag.Title = "性格类型测评-自评";
}

@section styles
{
     <style type="text/css">
        .img { width: 100%; }
        .cp-title { color: #14ae67; text-align: center; margin: 8px 0; }
        .cp-brief { font-size: 14px; margin: 5px 15px; line-height: 1.75em; border-top: 1px solid #14ae67; border-bottom: 1px solid #14ae67; padding-bottom: 10px; }
        .cp-brief p { text-indent: 2em; padding-top: 5px; }
        .cp-warn { margin: 5px 15px; }
        .warn-title { color: #e84c33; font-size: 16px; font-weight: bold; margin-bottom: 5px; }
        .warn-content { font-size: 14px; line-height: 1.75em; }
        .warn-content b { color: #0070C0;font-weight:bold; }
        .temp { height:100px; }
        .btnFooter { padding: 15px; background-color: rgba(0, 0, 0, .5); position: fixed; width: 100%; bottom: 53px; left: 0; }
        .btnFooter input { text-align: center; background: #14ae67; color: #fff; border: none; height: 40px; padding: 0 10px; line-height: 40px; cursor: pointer; min-width: 120px; font-size: 16px; display: inline-block; overflow: visible; border-radius: 3px; width: 98%; }
    </style>
}

        <div>
            <img class="img" src="@Url.Content("~/Images/IconImg/MBTITitle.png")">
            <div class="cp-title">性格类型测评（青少年自评版）</div>
            <div class="cp-brief">

                <p class="textindent">MBTI是世界上应用最广泛的性格测评工具，每年有200多万人接受测评。</p>
                <p class="textindent">MBTI主要建立在卡尔·荣格的感知与判断理论基础上，将人们大致分为16种类型，是一种辨别人与人异同点的简单方法。</p>
                <p class="textindent">本测评由“一苇教育”依据MBTI性格模型，针对10万青少年人群，历时十年之久研发而成。</p>
                <p class="textindent">通过本测评，青少年能了解自身的性格特征、偏好的学习模式、匹配的报考专业以及今后的职业方向，更好的进行人生职业生涯规划。</p>
            </div>

            <div class="cp-warn">
                <div class="warn-title">温馨提醒：</div>
                <div class="warn-content">
                    <p>本测评共28题（需时15分钟），<b>仅限中国青少年（13-18周岁）亲自测评</b>，谢绝家长代劳。</p>
                </div>
            </div>

            <div class="temp"></div>

            <div class="tc btnFooter">
                <input type="button" value="立即测试" class="btn-green btn-green-big"/>
            </div>

        </div>

@section scripts
{
    <script src="@Url.Content("~/Scripts/jquery-1.11.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/dialog.js")" type="text/javascript"></script>
    <script type="text/javascript">
        $().ready(function () {
            $(".btn-green").on("click", function() {
                $.ajax({
                    url: '@Url.Action("HadTest")',
                    type: 'post',
                    dataType: 'json',
                    data: { tid: 10 },
                    success: function(data) {
                        if (data.Id > 0 && data.ProReportCode) {
                            dialog.conform("温馨提醒", "您已经是专业版报告，重新测试后，将只有免费版报告，需要重新付费升级至专业版，是否重新测试？", "重新测试", "看看原来的报告", function () { location.href = '@Url.Action("Testing")'; }, function () { location.href = ''; });
                        } else if (data.Id > 0) {
                            dialog.conform("温馨提醒", "您已经在" + data.ProReportRemark + " 做过此测评，是否重新测试？", "重新测试", "看看原来的报告", function () { location.href = '@Url.Action("Testing")'; }, function () { location.href = ''; });
                        }
                        else {
                            window.location.href = '@Url.Action("Testing")';
                        }
                    },
                    error: function(e) {

                    },
                    complete: function() {
                    }
                });
            });
        });
    </script>
}
